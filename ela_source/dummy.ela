
#import core; // for println among many other common utilities.

A :: struct {
  B :: struct {
    #static Get :: () -> s32 {
      return 0;
    }
  }
}

main :: () {
  n := A::B::Get();
  println(to_string(n));
}

