
// declare a module (rest of this file will be inside this)
#mod std::mem;

Allocator :: #interface {
  #static alloc :: fn![T]() -> T*;
}

default_allocator: Allocator;

alloc :: fn![T, Alloc = std::default_allocator]() {
    return Alloc::alloc![T]();
  }

// declare a module (the block's contents will be inside it.)
#mod std::mem {
  alloc :: fn![T, Alloc = std::default_allocator]() {
    return Alloc::alloc![T]();
  }
}








