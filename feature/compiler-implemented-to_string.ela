

Strukt :: struct {
  a: int,
  b: int,
  c: string,
} 



main :: fn() {
  a : Strukt;
  // We should have a default implemented to_string on all structs,
  // unless the implementer provides one.
  println($"{a}");
}
