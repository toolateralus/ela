
Strukt :: struct {
  a: s32;
  b: s32;
  c: s32;
}

// Instead of just a confusing list of values, you can optionally use rust-like syntax for
// initializing a struct.
main :: fn() {
  strukt : Strukt = {
    a: 0,
    b: 0,
    c: 10
  }
}
