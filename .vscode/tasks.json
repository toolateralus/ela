{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile_current",
            "command": "${workspaceFolder}/bin/ela",
            "args": [
                "${file}"
            ]
        }
    ]
}