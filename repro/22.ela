#import core;

Struct :: struct {
  #static StaticMethod :: fn() -> int {
    return 10;
  }
}

CallMeMaybe:: fn![T]() {
  T::StaticMethod();
}

Strackt :: struct![T] {
  n := T::StaticMethod();
}

main :: fn() {
  CallMeMaybe![Struct]();
  println(to_string(f.n));
}
