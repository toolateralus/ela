Struct :: struct;
impl Struct {
  static_method :: fn() -> int {
    return 0;
  }
  other_method :: fn() {
    // #self can't begin an expression statement for some reason
    #self::static_method();
  }
}

main :: fn() {}