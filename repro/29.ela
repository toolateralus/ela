#import core;

main :: fn() {
  // * the call to sizeof looks like `sizeof(_tuple(int, int))`
  // * which is obviously totally wrong.
  n := malloc(sizeof((int, int)));

  // ? a hacky workaround :: 
  // * n := malloc(#type((int, int)).size);
}
