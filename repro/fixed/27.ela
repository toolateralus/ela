type Alias :: s64;

// Impl on an type crashes.
// probably a null scope.
// we should error for this, you probably don't need to impl an type, that makes no sense.
impl Alias {
  fn from(n: s32) -> Self {
    return n as Self;
  }
}

fn main() {}