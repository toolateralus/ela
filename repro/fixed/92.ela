fn format!<T>(string: str, tuple: T, options: s32) {

}

struct Vec2 {
  x: f32,
  y: f32,
}

impl Vec2 {
  fn format(self) {
    format("Vec2 { x: %, y: % }", (
      self.x,
      self.y,
    ), 1);
  }
}

fn main() {
  vec2: Vec2;
  vec2.format();
}

