import assertion::*;

fn main() { 
  mut list : List!<s32>;
  list.initialize(11);
  // index assignment via the operator overload is just totally broken.
  list[10] = 10;
  assert(list[10]==10, "");
}
