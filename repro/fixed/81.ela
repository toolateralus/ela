
import fmt::*;

fn main() { 
  // This just crahes the compiler?
  // We're passing Type* to println, which should be fine
  // it works if we pass the .name, but still what.
  println(typeof(*mut u8));
}