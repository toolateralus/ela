// !NO_COMPILE

// Another test that we cannot actuall test for regression because it needs to NOT compile.

fn main() { 
  // this is an insane example of how broken the freaking parser is in accordance to types
  // as well as the typer not caring whether a symbol is a _type or not.
  s: str = .{
    data: "strdup"c, // we inherit the _type from this symbol?
  };
}
