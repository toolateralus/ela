
main :: fn() {
  begin: u64;
  end: u64;

  // This error bleeds into c++, because it uses an initializer list
  // and cannot narrow u64 to s64.
  // Range should be a generic type anyway, we can have u8..u8 or u32..u32, etc.
  // Range![T] take one arg.
  for i in begin..end { 

  }
}
