Strukty :: struct {
  ptr: void*;
}

main :: fn() {
  n := 0;
  ptr := &n;


  // ! Compiler segfaults here, because we're not checking that the ASTType* base is a valid expression for the application.
  // ! We pass it an init list, which is blatantly incorrect.
  // ! only identifiers, and scope resolution expressions that end in an identifier or ![T, T1]
  // ! are valid names of types(?)
  str: Strukty { 
    ptr: ptr
  };
}