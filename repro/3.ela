Something :: struct;

Something :: struct {
  tag: int = 10;
}

forward :: (something : int) -> Something;

forward :: (something : int) -> Something {
  return #make(Something, {100});
}

#import core;
main :: () {
  println($"{forward(10).tag == 100}");
}
