
Struct :: struct {
  data: void*;
  length: s32;
  #operator(==) :: (other: Struct) -> bool {
    // Cyclic scope reference happens here
    // somehow other.data[0] causes us to parent Struct::scope -> Struct::scope;
    return data[0] != other.data[0];
  }
}