
// use of undeclared identifier local_func
// really, there's not much use to having local functions when we have quick lambdas.
// I guess for some really tight loop where you don't want a forced indirection??
// Nah, it's not neccesary. just write a scoped part of your code or extract a function.
test_expr_body_local_fn :: fn() {
  local_func :: fn() -> s32 => 150;
  n := local_func();
}

main :: fn() { 

}
