
#import core; // for println among many other common utilities.
#import file;

main :: () {
  args := Env::args();
  input := File::read_all("input/5.input");
  lines := Strings::split(input, '\n');

  rules := string[][]; // !BUG tries to use a type as the value.
  
  for line in lines {
    for ch in line {
      if ch == '|' then rules ~= Strings::split(line, '|');
    }
  }
}

