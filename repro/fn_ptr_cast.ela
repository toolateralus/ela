#import dlfcn;
#import core;

#alias some_function :: void(int)*;

something :: (int n) {
  println($"{n}");
}

main :: () {
  // !BUG Adding extensions to atype alias still freaking fails.
  // #alias some_function :: void(int);
  // some_function* some = &something;  
  some_function some = &something;  
  some(10);
}
