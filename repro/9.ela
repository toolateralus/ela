#import core;

Something :: struct {
  n: int;
  v: int;
  
  to_string :: () -> string {
    return $"{n}, {v}";
  }
}

SomeFunc :: () -> Something {
  return {0, 0};
}

main :: () {
  println($"{SomeFunc()}");  
}