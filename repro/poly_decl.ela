#import core; 

factorial :: ($T n) -> $T {
  if n == 0 {
    return 1;
  } else { return n * factorial(n - 1); }
}

/* 
  This fails because factorial never gets emitted when used in an interpolated string. 
  Strange!
*/

main :: ()  {
  println($"{factorial(5)}");
}