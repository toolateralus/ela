
/* 
  * Using  the type inference assignment operator `:=` on function pointer types is totally bugged.
  * The cpp code will look like 
  ? TYPE;
  * where obviously it needs to look like
  ? TYPE = VALUE;
*/

main :: ()  {
  #alias fn :: void()*;
  
  // ? This works fine
  f : fn = &main;
  
  // !this totally fails.
  f1 := &main;
}
