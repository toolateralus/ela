#import core;

GenericStruct :: struct<$T, int N> {
  int v = N;
  #ctor :: () {
    T value; // This fails, the compiler just segfaults.
  }
}

main :: () {
  GenericStruct<int, 100> josh;
  GenericStruct<int, 200> jos1h;
  
  printf("1=%d, 2=%d\n", josh.v, jos1h.v);
}