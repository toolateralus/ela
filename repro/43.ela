unwrap :: fn![T](tuple: (string, T)) -> T {
  a, b := tuple;
  sizeof(T); // "use of undeclared identifier T", this error leaks into C++. Somehow, this gets emitted as T instead of the instantiated type.
  return b;
}

main :: fn() { 
  unwrap![int](("Error", 100));
}
