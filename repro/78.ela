main :: fn() { 
  // We aren't applyign the operator overload for == on struct here,
  // we just try to use C's == operator. that's clearly messed up.
  switch str::from("blah") {
    str::from("blooh"): {}
  }

  // Again, range needs a == or .contains function applied for switches.
  switch 0 {
    0..10: {
    }
    10..20: {
    }
  }
}
