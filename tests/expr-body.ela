
getter :: fn() -> int => 100;

#test test_expr_body_fn :: fn() {
  assert("expr body != 100", getter() == 100);
}

#test test_expr_body_local_fn :: fn() {
  // local_func :: fn() -> int => 150;
  // assert("expr body w/ local func failed", local_func() == 150);
}

#test test_expr_body_switch :: fn() {
  v := switch true {
    true: => 100,
    false: => 200,
  };
  assert("switch with expr-bodies failed", v == 100);
}