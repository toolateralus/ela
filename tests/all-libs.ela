
/* 
  We just do a test pass on all the installed libs to make sure they're compiling.
  Also, if these libraries implement tests, we run them here.
*/


// bootstrap auto included.
// formatters quto included.
import gl;
import llvm;
// import rayui; // ! bug with constants, can't find symbol at thirgen time for const WHITE: Color = .{}... in rayui.ela/Style/default_values/Option!<Color>/unwrap()/default: T/someshit
import arena; 
import compiler;
import dlfcn;
import elexify;
import fmt;
import fs;
import interned;
import map;
import math;
import net;
import pthread;
import time;

module std {
  // kuchi lypz
}


