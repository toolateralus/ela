;
#import raylib;

DrawCommand :: struct {
  rect: Rectangle,
  color: Color,
}

DrawContext :: struct {
  commands: List![DrawCommand],
  inputState: bool[256],
}
impl DrawContext {
   draw :: fn(self*) {
    while self.commands.length > 0 {
      command := self.commands.pop();
      DrawRectangle(command.rect.x as int, command.rect.y as int, command.rect.width as int, command.rect.height as int, command.color);
    }
  }
}

#alias function :: fn* (DrawContext*);
