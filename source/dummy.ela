#import core;

Xaryu :: struct {
  float f;
  float f1;
  int x2;
}

main :: () {
  Type *type = #type Xaryu;
  printf("%s type info:\n", type.name);
  for field; type.fields {
    printf("...field: { name: %s, type=%s }\n", field.name, field.type.name);
    
  }
}