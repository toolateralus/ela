#import core;

Reflector :: struct {
  n: int;
}

main :: () {
 reflector: Reflector;
  
 type := #type Reflector;
 field := type.fields[0];
 
 integer: int = 100;
 field.set(&reflector, integer);
 
 println($"{reflector.n}");
}