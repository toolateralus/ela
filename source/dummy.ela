#import core;

Xaryu :: struct {
  float f;
  float f1;
  int x2;
}

main :: () {
  Type *type = #type Xaryu;
  printf("%s type info:\n", type.name);
  
  for field; type.fields {
    printf("%p\n", field.name);
    // printf("field: { name: %s , ttype_id=%d } ", field.name, field.type);
  }
}