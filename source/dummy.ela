

InitListTestCtorStruct :: struct {
  n : int;
  f : float;
  #ctor :: (n : int, f : float) {
    this.n = n;
    this.f = f;
  }
}

#test test_init_list_ctor :: () {
  i : InitListTestCtorStruct = {0, 1.0};
  assert("init list calling ctor failed", i.n == 0 && i.f == 1.0);
}
