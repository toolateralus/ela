#import core;

#alias FunctionPointer :: int(int, int)*;
function_pointer_helper :: (x: int, y) -> int { return x * y; }

main :: () {
  ptr: FunctionPointer = &function_pointer_helper;
  
  array: FunctionPointer[][];
  
  fixed: FunctionPointer[12];
  
  
  function :: () -> FunctionPointer {
    return null;
  }
  
  printf("%d\n", ptr(4, 4));
}
