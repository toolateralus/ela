#import core;

// BUG(Josh) 9/30/2024, 3:33:33 PM
// TODO: figure out why we can't parse type extensions *, [], on aliases.

#alias VoidFunction :: void();

#alias Xaryu :: int*;

int n;

main :: () {
  VoidFunction** func = #make(VoidFunction**, malloc(sizeof(VoidFunction*) * 10));
  for s32 i; i < 10; ++i {
    func[i] = &main;
  }
  printf("%p\n", func);
  
}